pipeline {
       agent any
       // hello2
       when {
                    changeset "second/*.*"
        } 
       stages {
           stage('Hello 2') {
            //    when {
            //         changeset "second/*.*"
            //    }
               steps {
                   sh 'cat second/README.md'
               }
           }
       }
   }